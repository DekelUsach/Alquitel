<Window x:Class="Alquitel.UI.Views.InputPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ingresar valor"
        Width="400" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        FontFamily="{StaticResource AppFontFamily}"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Border Padding="28,24">
        <StackPanel>
            <TextBlock x:Name="TitleText" FontSize="16" FontWeight="Bold"
                       Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
            <TextBlock x:Name="HintText" FontSize="12"
                       Foreground="{DynamicResource MutedTextBrush}"
                       TextWrapping="Wrap" Margin="0,0,0,18"/>

            <Border Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1" CornerRadius="10" Margin="0,0,0,14">
                <TextBox x:Name="ValueInput" Padding="14,10" FontSize="14"
                         Background="Transparent" BorderThickness="0"
                         Foreground="{DynamicResource TextBrush}"
                         CaretBrush="{DynamicResource TextBrush}"
                         MaxLength="80"/>
            </Border>

            <Button Content="Aceptar" Style="{StaticResource PrimaryButton}"
                    Height="42" Margin="0,0,0,8" Click="Accept_Click" IsDefault="True"/>
            <Button Content="Cancelar" Style="{StaticResource GhostButton}"
                    Height="38" IsCancel="True"/>
        </StackPanel>
    </Border>
</Window>
